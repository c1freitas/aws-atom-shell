{
  "name": "aws-app",
  "version": "0.0.1",
  "description": "aws instance example with Atom Shell",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chuck freitas",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1.8"
  }
}
